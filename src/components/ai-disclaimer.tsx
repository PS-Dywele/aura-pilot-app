export function AiDisclaimer() {
  return (
    <p className="mt-4 border-t border-border/60 pt-3 text-xs leading-relaxed text-muted-foreground">
      AI-generated content may contain errors. Always review before use.
    </p>
  );
}

export function AppFooter() {
  return (
    <footer className="border-t border-border/60 px-6 py-5 text-center text-xs leading-relaxed text-muted-foreground">
      Responsible AI — this tool assists, it does not replace professional judgement. No user data is
      stored or shared.
    </footer>
  );
}